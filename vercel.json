{
  "buildCommand": "npm run build --prefix application-portal",
  "outputDirectory": "application-portal/dist",
  "installCommand": "npm install --prefix application-portal",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}

